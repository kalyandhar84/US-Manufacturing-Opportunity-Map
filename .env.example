# Optional. Free Census key: https://api.census.gov/data/key_signup.html
# Without it, refresh_data.py still updates EPA TRI, USDA FSIS, and OSHA ITA.
# On Azure, set the same name as an App Setting if you want ACS metro scores to blend.
CENSUS_API_KEY=
